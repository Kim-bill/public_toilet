---
phase: 01-map-and-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gas/Code.gs
  - src/js/api.js
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
user_setup:
  - service: google-apps-script
    why: "Serverless backend for toilet data CRUD"
    env_vars: []
    account_setup:
      - task: "Create a Google Sheet named 'public_toilet' with a sheet tab named 'toilets'"
        location: "Google Sheets (sheets.google.com)"
      - task: "Add header row with columns: id, buildingName, floor, locationMemo, hasLock, password, latitude, longitude, region, category, createdAt, updatedAt"
        location: "First row of 'toilets' sheet"
      - task: "Enter seed data rows (provided in gas/Code.gs comments) into the sheet"
        location: "'toilets' sheet, rows 2+"
      - task: "Open Apps Script editor (Extensions > Apps Script), paste gas/Code.gs, deploy as Web App (Execute as: Me, Access: Anyone)"
        location: "Google Apps Script editor at script.google.com"
      - task: "Copy the deployed Web App URL and paste it into src/js/api.js replacing the GAS_URL placeholder"
        location: "src/js/api.js line 3"
  - service: kakao-developers
    why: "Map rendering API key"
    account_setup:
      - task: "Register app at Kakao Developers Console, get JavaScript key"
        location: "https://developers.kakao.com/ > My Application > App Keys"
      - task: "Add platform: Web, with site domains 'http://localhost' and 'https://USERNAME.github.io'"
        location: "Kakao Developers > My Application > Platform > Web"
      - task: "Copy JavaScript key and paste into index.html replacing YOUR_KAKAO_JS_KEY"
        location: "index.html script tag"
must_haves:
  truths:
    - "GAS Code.gs can read all toilet data from Google Sheets and return JSON"
    - "Frontend api.js can fetch toilet data from GAS endpoint"
    - "Seed data for major Misa New Town buildings exists in Google Sheets"
  artifacts:
    - path: "gas/Code.gs"
      provides: "GAS backend with doGet endpoint"
      contains: "function doGet"
    - path: "src/js/api.js"
      provides: "Frontend GAS fetch wrapper"
      exports: ["getToilets"]
  key_links:
    - from: "src/js/api.js"
      to: "GAS Web App URL"
      via: "fetch with redirect: follow"
      pattern: "fetch.*GAS_URL"
    - from: "gas/Code.gs"
      to: "Google Sheets 'toilets' tab"
      via: "SpreadsheetApp.getActiveSpreadsheet"
      pattern: "getSheetByName.*toilets"
---

<objective>
Create the Google Apps Script backend and frontend API layer for reading toilet data from Google Sheets.

Purpose: Validates the GAS integration (the second critical external dependency) and provides the data layer that Plan 03 consumes to render markers.
Output: GAS Code.gs ready for deployment, frontend api.js fetch wrapper, and seed data specification for Misa New Town buildings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-map-and-data/01-CONTEXT.md
@.planning/phases/01-map-and-data/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GAS backend with doGet endpoint and seed data</name>
  <files>gas/Code.gs</files>
  <read_first>
    .planning/phases/01-map-and-data/01-RESEARCH.md
    .planning/phases/01-map-and-data/01-CONTEXT.md
  </read_first>
  <action>
Create `gas/Code.gs` with the following functions. This file is NOT deployed by Vite -- it is manually pasted into the Google Apps Script editor by the user.

1. `doGet(e)` function:
   - Read `e.parameter.action`
   - If action === 'getAll', call `getAllToilets()` and return result
   - Default: return `jsonResponse({ error: 'Unknown action' })`

2. `getAllToilets()` function:
   - Get sheet: `SpreadsheetApp.getActiveSpreadsheet().getSheetByName('toilets')`
   - Read all data: `sheet.getDataRange().getValues()`
   - First row is headers, skip it
   - Map each row to object with fields per D-06:
     - `id`: row[0] (string)
     - `buildingName`: row[1] (string)
     - `floor`: row[2] (string)
     - `locationMemo`: row[3] (string)
     - `hasLock`: `row[4] === true || row[4] === 'TRUE'` (boolean)
     - `password`: row[5] (string)
     - `latitude`: `parseFloat(row[6])` (number)
     - `longitude`: `parseFloat(row[7])` (number)
     - `region`: row[8] (string)
     - `category`: row[9] (string)
     - `createdAt`: row[10] (string)
     - `updatedAt`: row[11] (string)
   - Skip rows where `row[0]` is empty
   - Return `jsonResponse({ status: 'ok', data: toilets })`

3. `jsonResponse(data)` function:
   - Return `ContentService.createTextOutput(JSON.stringify(data)).setMimeType(ContentService.MimeType.JSON)`

4. Add a comment block at the top of the file with seed data to enter into Google Sheets (per D-04, D-05):
   ```
   // === SEED DATA FOR GOOGLE SHEETS ===
   // Enter these rows into the 'toilets' sheet after the header row.
   // Passwords are left empty per D-05 (to be filled after visiting).
   //
   // Headers: id | buildingName | floor | locationMemo | hasLock | password | latitude | longitude | region | category | createdAt | updatedAt
   //
   // 1 | 스타필드 미사 | 1F | 1층 중앙 에스컬레이터 옆 | TRUE | | 37.5604 | 127.1802 | 미사신도시 | 상업시설 | 2026-04-08 |
   // 2 | 스타필드 미사 | B1 | 지하1층 푸드코트 옆 | TRUE | | 37.5604 | 127.1802 | 미사신도시 | 상업시설 | 2026-04-08 |
   // 3 | 홈플러스 미사점 | 1F | 정문 입구 오른쪽 | FALSE | | 37.5631 | 127.1795 | 미사신도시 | 상업시설 | 2026-04-08 |
   // 4 | 이마트 미사점 | 1F | 계산대 뒤편 | TRUE | | 37.5589 | 127.1823 | 미사신도시 | 상업시설 | 2026-04-08 |
   // 5 | 미사역 | B1 | 지하1층 대합실 | FALSE | | 37.5600 | 127.1800 | 미사신도시 | 공공시설 | 2026-04-08 |
   // 6 | 미사강변공원 | 1F | 공원 입구 공중화장실 | FALSE | | 37.5567 | 127.1756 | 미사신도시 | 공공시설 | 2026-04-08 |
   // 7 | 미사역 상가 A동 | 2F | 엘리베이터 옆 | TRUE | | 37.5596 | 127.1808 | 미사신도시 | 카페/음식점 | 2026-04-08 |
   // 8 | 미사역 상가 B동 | 1F | 편의점 옆 통로 | TRUE | | 37.5598 | 127.1812 | 미사신도시 | 카페/음식점 | 2026-04-08 |
   ```
   Note: These coordinates are approximate for Misa New Town area. The user should verify exact coordinates using Kakao Maps search during data entry.
  </action>
  <verify>
    <automated>cat "D:/D_Chapter2/DEV/Antigravity_Dev/Private/public_toilet/gas/Code.gs" | grep -c "function"</automated>
  </verify>
  <acceptance_criteria>
    - `gas/Code.gs` contains `function doGet(e)`
    - `gas/Code.gs` contains `function getAllToilets()`
    - `gas/Code.gs` contains `function jsonResponse(data)`
    - `gas/Code.gs` contains `getSheetByName('toilets')`
    - `gas/Code.gs` contains `ContentService.createTextOutput`
    - `gas/Code.gs` contains `setMimeType(ContentService.MimeType.JSON)`
    - `gas/Code.gs` contains seed data comment with at least 8 entries (스타필드, 홈플러스, 이마트, 미사역, 미사강변공원, 상가)
    - `gas/Code.gs` contains column mapping for all 12 fields: id, buildingName, floor, locationMemo, hasLock, password, latitude, longitude, region, category, createdAt, updatedAt
  </acceptance_criteria>
  <done>GAS backend code ready to paste into Google Apps Script editor; seed data documented in comments</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend API fetch wrapper</name>
  <files>src/js/api.js</files>
  <read_first>
    .planning/phases/01-map-and-data/01-RESEARCH.md
  </read_first>
  <action>
Create `src/js/api.js` with a single exported function for fetching toilet data from GAS.

```javascript
// GAS Web App URL -- replace with your deployed URL
// Deploy: Google Apps Script Editor > Deploy > New Deployment > Web App
const GAS_URL = 'YOUR_GAS_DEPLOYMENT_URL';

/**
 * Fetch all toilet data from Google Sheets via GAS.
 * @returns {Promise<Array>} Array of toilet objects
 * @throws {Error} If fetch fails or response is not ok
 */
export async function getToilets() {
  const response = await fetch(`${GAS_URL}?action=getAll`, {
    method: 'GET',
    redirect: 'follow',
  });
  if (!response.ok) {
    throw new Error(`Failed to fetch toilet data: ${response.status}`);
  }
  const result = await response.json();
  if (result.status !== 'ok') {
    throw new Error(result.error || 'Unknown API error');
  }
  return result.data;
}
```

Key details:
- `redirect: 'follow'` is critical -- GAS responds with a 302 redirect that fetch must follow transparently
- The GAS_URL placeholder will be replaced by the user after deploying Code.gs (per user_setup)
- Export only `getToilets()` -- all GAS communication goes through this single function
- Check both HTTP status AND the `status` field in the JSON response
  </action>
  <verify>
    <automated>cat "D:/D_Chapter2/DEV/Antigravity_Dev/Private/public_toilet/src/js/api.js" | grep -c "export"</automated>
  </verify>
  <acceptance_criteria>
    - `src/js/api.js` contains `export async function getToilets()`
    - `src/js/api.js` contains `GAS_URL`
    - `src/js/api.js` contains `redirect: 'follow'`
    - `src/js/api.js` contains `action=getAll`
    - `src/js/api.js` contains `response.json()`
    - `src/js/api.js` contains `result.data`
    - `src/js/api.js` does NOT contain any other exported functions (single responsibility)
  </acceptance_criteria>
  <done>Frontend API layer exports getToilets() that fetches from GAS with proper redirect handling and error checking</done>
</task>

</tasks>

<verification>
- `gas/Code.gs` contains doGet, getAllToilets, jsonResponse functions
- `gas/Code.gs` maps all 12 columns from D-06
- `src/js/api.js` exports getToilets() with redirect: 'follow'
- Seed data covers all D-04 categories: commercial (스타필드, 홈플러스, 이마트), public (미사역, 미사강변공원), cafe/restaurant (미사역 상가)
</verification>

<success_criteria>
- GAS Code.gs is complete and ready for manual deployment
- Frontend api.js correctly wraps GAS fetch with error handling
- Seed data covers 8+ buildings across commercial, public, and dining categories per D-04
- All data fields match D-06 column structure exactly
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-and-data/01-02-SUMMARY.md`
</output>
