---
phase: 01-map-and-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - index.html
  - src/main.js
  - src/js/map.js
  - src/css/style.css
  - .github/workflows/deploy.yml
autonomous: true
requirements: [MAP-01]
must_haves:
  truths:
    - "App loads and displays a Kakao Map centered on Misa New Town"
    - "Map fills the entire viewport on mobile"
    - "GitHub Actions deploy workflow exists for GitHub Pages"
  artifacts:
    - path: "index.html"
      provides: "Single-page entry point with Kakao SDK script tag"
      contains: "dapi.kakao.com/v2/maps/sdk.js"
    - path: "src/js/map.js"
      provides: "Kakao Maps initialization"
      exports: ["initMap"]
    - path: "vite.config.js"
      provides: "Vite build config with GitHub Pages base path"
      contains: "base: '/public_toilet/'"
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Pages deployment workflow"
      contains: "deploy-pages"
  key_links:
    - from: "index.html"
      to: "src/main.js"
      via: "script module import"
      pattern: 'type="module".*src/main.js'
    - from: "src/main.js"
      to: "src/js/map.js"
      via: "ES module import"
      pattern: "import.*from.*map"
---

<objective>
Scaffold the Vite project and render a full-screen Kakao Map centered on Misa New Town.

Purpose: Validates the most critical integration (Kakao Maps SDK loading on a Vite-built static site) and establishes the project structure all subsequent plans build on.
Output: Working Vite project with full-screen Kakao Map, Pico CSS installed, and GitHub Actions deploy workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-map-and-data/01-CONTEXT.md
@.planning/phases/01-map-and-data/01-RESEARCH.md
@.planning/phases/01-map-and-data/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project with Kakao Map rendering</name>
  <files>package.json, vite.config.js, index.html, src/main.js, src/js/map.js, src/css/style.css</files>
  <read_first>
    .planning/phases/01-map-and-data/01-RESEARCH.md
    .planning/phases/01-map-and-data/01-UI-SPEC.md
  </read_first>
  <action>
Create the Vite vanilla JS project from scratch in the repo root (the repo root IS the project root -- do not create a subdirectory).

1. Initialize with `npm init -y` and install dependencies:
   - `npm install vite@latest` (dev dependency)
   - `npm install @picocss/pico`

2. Create `vite.config.js`:
   ```javascript
   import { defineConfig } from 'vite';
   export default defineConfig({
     base: '/public_toilet/',
     build: { outDir: 'dist' },
   });
   ```

3. Create `package.json` scripts:
   - `"dev": "vite"`
   - `"build": "vite build"`
   - `"preview": "vite preview"`

4. Create `index.html` in project root:
   - Load Kakao Maps SDK via script tag BEFORE the module script: `<script src="//dapi.kakao.com/v2/maps/sdk.js?appkey=YOUR_KAKAO_JS_KEY"></script>`
   - Use placeholder `YOUR_KAKAO_JS_KEY` for now (per D-08, will be replaced with real key when user provides it)
   - Include a `<div id="map" style="width:100%;height:100vh;"></div>` as the map container
   - Include `<div id="bottom-sheet">` and `<div id="backdrop">` elements (empty for now, wired in Plan 03)
   - Include bottom sheet HTML structure per UI-SPEC:
     - Drag handle div with class `drag-handle`
     - `<h2 id="bs-building"></h2>` for building name
     - Lock status badge: `<span id="bs-lock" class="lock-badge"></span>`
     - Detail fields using `<dl>`: floor (`#bs-floor`), location memo (`#bs-memo`), password (`#bs-password`)
   - Include loading overlay: `<div id="loading-overlay">` with spinner and text "화장실 정보를 불러오는 중..."
   - Include error state: `<div id="error-state">` with heading "데이터를 불러올 수 없습니다", body "인터넷 연결을 확인하고 다시 시도해 주세요.", retry button "다시 시도"
   - Load `src/main.js` as `type="module"`
   - Set viewport meta: `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
   - Set `<html lang="ko">`

5. Create `src/js/map.js`:
   ```javascript
   export function initMap(containerId) {
     const container = document.getElementById(containerId);
     const options = {
       center: new kakao.maps.LatLng(37.5600, 127.1800),
       level: 5
     };
     return new kakao.maps.Map(container, options);
   }
   ```

6. Create `src/main.js`:
   - Import `initMap` from `./js/map.js`
   - Call `initMap('map')` on DOMContentLoaded
   - For now, just initialize the map (data fetching and markers come in Plan 03)

7. Create `src/css/style.css`:
   - Import Pico CSS: `@import '@picocss/pico';` (Vite resolves node_modules imports in CSS)
   - Add full-screen map styles: `html, body { margin: 0; padding: 0; overflow: hidden; height: 100%; }`
   - Add `#map { width: 100%; height: 100vh; }` 
   - Add bottom sheet CSS from UI-SPEC (the full CSS block including #backdrop, #bottom-sheet, .drag-handle, .open/.visible classes, reduced-motion media query, and 480px breakpoint)
   - Add loading overlay styles: fixed position, full-screen, white background with 0.85 opacity, centered content, z-index: 20
   - Add error state styles: centered card on map, z-index: 15
   - Add lock badge styles: `.lock-badge.locked { background: #fdedec; color: #c0392b; }` and `.lock-badge.unlocked { background: #eafaf1; color: #27ae60; }`
   - Add password display style: `#bs-password { color: #2c3e50; font-family: monospace; font-weight: 700; }`
   - Import style.css in index.html via `<link rel="stylesheet" href="/src/css/style.css">`
  </action>
  <verify>
    <automated>cd "D:/D_Chapter2/DEV/Antigravity_Dev/Private/public_toilet" && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"vite"` in devDependencies and `"@picocss/pico"` in dependencies
    - `vite.config.js` contains `base: '/public_toilet/'`
    - `index.html` contains `dapi.kakao.com/v2/maps/sdk.js`
    - `index.html` contains `id="map"` and `id="bottom-sheet"` and `id="backdrop"` and `id="loading-overlay"` and `id="error-state"`
    - `index.html` contains `lang="ko"`
    - `src/js/map.js` contains `export function initMap`
    - `src/js/map.js` contains `37.5600` and `127.1800` and `level: 5`
    - `src/main.js` contains `import` and `initMap`
    - `src/css/style.css` contains `@import` and `pico`
    - `src/css/style.css` contains `#bottom-sheet` and `#backdrop` and `translateY`
    - `src/css/style.css` contains `#loading-overlay`
    - `src/css/style.css` contains `.lock-badge.locked` and `.lock-badge.unlocked`
    - `npx vite build` completes without errors
  </acceptance_criteria>
  <done>Vite project builds successfully with Kakao Map initialization code, full bottom sheet HTML/CSS structure, loading overlay, error state, and Pico CSS integrated</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions deployment workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    .planning/phases/01-map-and-data/01-RESEARCH.md
  </read_first>
  <action>
Create `.github/workflows/deploy.yml` with the exact workflow from the research doc:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: ['main']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v4
        with:
          path: './dist'
      - id: deployment
        uses: actions/deploy-pages@v5
```

Also create a `.gitignore` file with standard Node/Vite entries:
```
node_modules
dist
.DS_Store
*.local
```
  </action>
  <verify>
    <automated>cat "D:/D_Chapter2/DEV/Antigravity_Dev/Private/public_toilet/.github/workflows/deploy.yml" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` exists
    - `.github/workflows/deploy.yml` contains `deploy-pages@v5`
    - `.github/workflows/deploy.yml` contains `npm run build`
    - `.github/workflows/deploy.yml` contains `path: './dist'`
    - `.gitignore` contains `node_modules` and `dist`
  </acceptance_criteria>
  <done>GitHub Actions workflow ready for GitHub Pages deployment; .gitignore configured</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds without errors
- `index.html` loads Kakao Maps SDK script tag
- `src/js/map.js` exports `initMap` with Misa New Town coordinates
- `.github/workflows/deploy.yml` is valid YAML
- Bottom sheet HTML structure and CSS are in place (ready for wiring in Plan 03)
</verification>

<success_criteria>
- Vite project builds to `dist/` directory
- Map initialization code targets Misa New Town center (37.5600, 127.1800) at zoom level 5
- All UI shells (bottom sheet, loading overlay, error state) exist in HTML with corresponding CSS
- GitHub Pages deploy workflow configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-and-data/01-01-SUMMARY.md`
</output>
